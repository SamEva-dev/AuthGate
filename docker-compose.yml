services:
  authgate.auth:
    image: ${DOCKER_REGISTRY-}authgateauth
    build:
      context: .
      dockerfile: src/AuthGate.Auth/Dockerfile

  authgate.mgmt:
    image: ${DOCKER_REGISTRY-}authgatemgmt
    build:
      context: src/AuthGate.Mgmt
      dockerfile: Dockerfile

