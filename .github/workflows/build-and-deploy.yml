name: AuthGate - Build & Promote (staging -> preprod -> prod)

on:
  push:
    branches: ["main"]

concurrency:
  group: authgate-main
  cancel-in-progress: false

jobs:
  build_push:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image tag
        id: meta
        shell: bash
        run: |
          echo "image_tag=sha-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push (immutable)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/authgate:${{ steps.meta.outputs.image_tag }}

  deploy_staging:
    runs-on: ubuntu-latest
    needs: [build_push]
    environment: staging
    steps:
      - name: Deploy to STAGING (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 60s
          command_timeout: 30m
          script: |
            set -euo pipefail

            IMAGE_TAG="${{ needs.build_push.outputs.image_tag }}"
            ROOT="/opt/locaguest"
            ENV_FILE="${ROOT}/env/.env"
            COMPOSE_FILE="${ROOT}/config/docker-compose.yml"
            mkdir -p "${ROOT}/env"
            touch "${ENV_FILE}"
            cd "${ROOT}"

            # Ensure dockerhub auth (for private repos; harmless if public)
            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            # Upsert KEY=VALUE into /opt/locaguest/env/.env (no secrets printed)
            upsert_env () {
              local key="$1"
              local value="$2"
              if grep -qE "^${key}=" "${ENV_FILE}"; then
                sed -i "s|^${key}=.*|${key}=${value}|" "${ENV_FILE}"
              else
                echo "${key}=${value}" >> "${ENV_FILE}"
              fi
            }

            upsert_env "AUTHGATE_TAG_STAGING" "${IMAGE_TAG}"

            # Bring up edge proxy if not already running
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" up -d caddy

            # Pull + up staging (ensure DB is present, no --no-deps)
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile staging pull authgate-staging
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile staging up -d authgate-db-staging authgate-staging

            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile staging ps

  deploy_preprod:
    runs-on: ubuntu-latest
    needs: [deploy_staging]
    environment: preprod
    steps:
      - name: Promote to PREPROD (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 60s
          command_timeout: 30m
          script: |
            set -euo pipefail

            IMAGE_TAG="sha-${{ github.sha }}"
            ROOT="/opt/locaguest"
            ENV_FILE="${ROOT}/env/.env"
            COMPOSE_FILE="${ROOT}/config/docker-compose.yml"
            mkdir -p "${ROOT}/env"
            touch "${ENV_FILE}"
            cd "${ROOT}"

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            upsert_env () {
              local key="$1"
              local value="$2"
              if grep -qE "^${key}=" "${ENV_FILE}"; then
                sed -i "s|^${key}=.*|${key}=${value}|" "${ENV_FILE}"
              else
                echo "${key}=${value}" >> "${ENV_FILE}"
              fi
            }

            upsert_env "AUTHGATE_TAG_PREPROD" "${IMAGE_TAG}"

            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" up -d caddy
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile preprod pull authgate-preprod
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile preprod up -d authgate-db-preprod authgate-preprod
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile preprod ps

  deploy_prod:
    runs-on: ubuntu-latest
    needs: [deploy_preprod]
    environment: prod
    steps:
      - name: Promote to PROD (VPS)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 60s
          command_timeout: 30m
          script: |
            set -euo pipefail

            IMAGE_TAG="sha-${{ github.sha }}"
            ROOT="/opt/locaguest"
            ENV_FILE="${ROOT}/env/.env"
            COMPOSE_FILE="${ROOT}/config/docker-compose.yml"
            mkdir -p "${ROOT}/env"
            touch "${ENV_FILE}"
            cd "${ROOT}"

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            upsert_env () {
              local key="$1"
              local value="$2"
              if grep -qE "^${key}=" "${ENV_FILE}"; then
                sed -i "s|^${key}=.*|${key}=${value}|" "${ENV_FILE}"
              else
                echo "${key}=${value}" >> "${ENV_FILE}"
              fi
            }

            upsert_env "AUTHGATE_TAG_PROD" "${IMAGE_TAG}"

            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" up -d caddy
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile prod pull authgate-prod
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile prod up -d authgate-db-prod authgate-prod
            docker compose -f "${COMPOSE_FILE}" --env-file "${ENV_FILE}" --profile prod ps
